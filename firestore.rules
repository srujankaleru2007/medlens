rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All sensitive access goes through the server, which validates tokens and ownership.
    // Admin SDK bypasses rules; the route authorization is mandatory.
    match /{document=**} { allow read, write: if false; }
  }
}
